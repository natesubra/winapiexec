name: Build

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1

      - name: Navigate to Workspace
        run: cd $GITHUB_WORKSPACE

      - name: getrefs
        id: getrefs
        run: |
          echo ::set-output name=SOURCE_NAME::${GITHUB_REF#refs/*/}
          echo ::set-output name=SOURCE_BRANCH::${GITHUB_REF#refs/heads/}
          echo ::set-output name=SOURCE_TAG::${GITHUB_REF#refs/tags/}

      - name: Build
        run: .\build.bat

      - name: Zip
        shell: bash
        env:
          _TAG: ${{ steps.getrefs.outputs.SOURCE_TAG }}
        run: |
          git fetch -a
          tag="$(git describe --tags --abbrev=0)"

          release_name="winapiexec-${tag}"

          # Pack files
          7z a -tzip "${release_name}-x86.zip" "./Release/winapiexec.exe"
          7z a -tzip "${release_name}-x64.zip" "./x64/Release/winapiexec.exe"

      - name: Publish
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: "*.zip"
          tag_name: ${{ steps.getrefs.outputs.SOURCE_TAG }}
          draft: false
          prerelease: false
